import { View } from '@/components/Themed';
import BhavList from '@/components/BhavList';

export default function RiceScreen() {
  return (
    <View style={{ flex: 1 }}>
      <BhavList segment={'Rice'} />
    </View>
  );
}